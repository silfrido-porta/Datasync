<section class="hero" id="inicio">
    <picture>
        <source srcset="/banner-1.webp" media="(max-width: 768px)" />
        <img src="/banner-1.webp" alt="Imagen de Servidores" class="img-banner" />
    </picture>
    <div class="wrapper-hero">
        <h1 class="title-hero">Gestión de soluciones tecnológicas integrales.</h1>
        <p class="text-hero">Dedicados al diseño, implementación, mantenimiento y gestión de soluciones tecnológicas integrales.</p>
        <a href="#nosotros" class="btn-contacto">
            <span>Conocé más</span>
        </a>
    </div>
</section>
<!-- Dedicados al diseño, implementación, mantenimiento y gestión de soluciones tecnológicas integrales.  -->


<style>
    .hero{
        position: relative;
        width: 100%;
        border-radius: var(--border-radius);
    }
    .hero:after{
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(rgba(0,0,0,.1),rgb(3 7 18 / 1));
    }
    .img-banner{
        width: 100%;
        height: 90vh;
        object-fit: cover;
    }
    .wrapper-hero{
        position: absolute;
        top: 56%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 1;
    }
    .title-hero{
        /* font-size: var(--font-hero); */
        font-size: calc(42px + (60 - 42) * (100vw - 768px) / (1440 - 768));
        letter-spacing: 0.04rem;
        line-height: 1.2;
        text-align: center;
        margin-bottom: 1.2rem;
        background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
    }
    .text-hero{
        font-size: 1.4rem;
        color: var(--white);
        letter-spacing: 0.04rem;
        line-height: 1.5;
        text-align: center;
        width: 70%;
        margin: 0 auto;
        text-wrap: pretty;
    }
    .btn-contacto{
        position: relative;
        width: fit-content;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #0f1014b3;
        border: 1px solid rgba(255, 255, 255, .1);

        padding: 2px;
        margin-top: 1.8rem;
        margin-left: auto;
        margin-right: auto;
        overflow: hidden;
        border-radius: 50px;

        text-decoration: none;
        transition: all .6s;
    }

    .btn-contacto span{
        font-size: 1.18rem;
        color: var(--white);
        letter-spacing: 0.04rem;
        line-height: 1.5;
        text-align: center;

        padding: .8rem 2rem;
        border-radius: 50px;

        background: #0f1014b3;
        -webkit-backdrop-filter: blur(20px);
        backdrop-filter: blur(20px);
        position: relative;
        z-index: 2;
        transition: all .8s;
    }

    .btn-contacto::after{
        content: '';
        position: absolute;
        width: 100%;
        min-height: 16rem;
        transform: rotate(-55deg);
        animation: spin 6s linear infinite;
        background: conic-gradient(from 0 at 50% 50%, rgba(38, 172, 221, .5) 0deg, rgba(38, 172, 221, 0) 60deg, rgba(38, 172, 221, 0) 310deg, rgba(38, 172, 221, .5) 360deg);
        z-index: 1;
    }

    .btn-contacto:hover{
        color: var(--white);
        border: 1px solid rgba(255, 255, 255, .5);
        background: rgba(38, 172, 221, .3);
        transition: all .6s;
    }

    @keyframes spin {
        0% {
        transform: rotate(0deg);
        }

        100% {
            transform: rotate(360deg);
        }
    }

    @media screen and (max-width:1200px) {
        .wrapper-hero{
            width: 80%;
            top: 60%;
            margin: 0 auto;
        }
    }
    @media screen and (max-width:768px) {
        .img-banner{
            height: 70vh;
        }
        .wrapper-hero{
            width: 98%;
            top: 60%;
            margin: 0 auto;
        }
        .text-hero{
            font-size: 1rem;
            width: 100%;
        }
        .btn-contacto{
            font-size: 1rem;
        }
    }

</style>